- hosts: localhost
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_cache: yes
      changed_when: False

  tasks:
    - include: tasks/cron-jobs.yml
    - include: tasks/test.yml