- name: Install ansible-pull cron job
  cron:
    user: siobrien 
    name: "ansible provision" 
    minute: "*/10" 
    job: "/usr/bin/ansible-pull -o -v -U https://github.com/ob1-sc/mint-blade-laptop-setup.git > /dev/null"
